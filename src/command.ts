export enum Command {
  StartWatching = 'autoMark.startWatching',
  StopWatching = 'autoMark.stopWatching',
  UpdateSnapshot = 'autoMark.updateSnapshot',
  ToggleWatching = 'autoMark.toggleWatching',
}
